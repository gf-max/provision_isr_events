{
  "config": {
    "step": {
      "user": {
        "title": "Provision-ISR ONVIF Events",
        "description": "Enter ONVIF parameters of your Provision-ISR NVR.",
        "data": {
          "host": "Host/IP",
          "port": "Port (default 80)",
          "username": "Username",
          "password": "Password"
        }
      }
    }
  },
  "options": {
    "step": {
      "basic": {
        "title": "Options",
        "description": "Global settings and channel list.",
        "data": {
          "auto_off": "Auto OFF (seconds)",
          "channels": "Channels (e.g. 1,2,6) — empty = unknown",
          "snapshot_on_motion": "Take snapshot on motion (default)",
          "snapshot_dir": "Snapshot directory (relative to /config, e.g. www/snapshots)",
          "snapshot_min_interval": "Minimum interval between snapshots (seconds)",
          "retention_days": "Retention (days)",
          "retention_max_files": "Maximum files per channel",
          "keep_latest_only": "Keep only the latest snapshot per channel"
        }
      },
      "channels": {
        "title": "Channels",
        "description": "Configure per-channel snapshot toggle for: {channels}"
      }
    }
  }
}
